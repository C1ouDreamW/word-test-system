# 专业英语单词测试系统 — 分工与时间表

> **选题**：4．专业英语单词测试（2人，A级）  
> **周期**：6/21（日）— 6/28（日），8 天  
> **策略**：先用 4~5 天集中搞定后端，最后 3 天写前端

---

## 一、分工

| 成员 | 后端 | 前端 |
|------|------|------|
| **A（组长）** | 考试 CRUD、组卷+随机打乱、自动判卷、统计查询、字符云数据 | 答题页、交卷结果页 |
| **B** | 单词 CRUD、登录、用户查询 | 登录页、学生首页、单词管理、考试管理、成绩统计、字符云 |

---

## 二、时间表

### 第 1 天 — 6/21（日）：项目搭建 + 数据库 + 实体类

> 进度：**5/5** ✅

- [x] 创建 Spring Boot 项目（Maven），配置 pom.xml、application.properties — A
- [x] 设计数据库表结构，编写建表 SQL，MySQL 中执行 — A + B
- [x] 编写所有实体类（entity 包） — A + B
- [x] 编写公共响应类 `ApiResult`（code / message / data） — A
- [x] 启动项目，确认 `localhost:8080` 可访问 — A

---

### 第 2 天 — 6/22（一）：DAO 层 + 登录

> 进度：**6/6** ✅

- [x] 编写 `WordDao` 接口 + 实现类 — B
- [x] 编写 `UserDao` 接口 + 实现类 — B
- [x] 编写 `ExamDao` 接口 + 实现类 — A
- [x] 编写 `QuestionDao` / `AnswerDao` 接口 + 实现类 — A
- [x] 编写 `UserService` + `UserController`（登录接口 `/api/login`） — B
- [x] 自测 DAO：验证数据库读写正常 — A + B

---

### 第 3 天 — 6/23（二）：单词管理 + 考试管理后端

> 进度：**5/5** ✅

- [x] 编写 `WordService`（CRUD + 搜索） — B
- [x] 编写 `WordController`（GET/POST/PUT/DELETE `/api/words`） — B
- [x] 编写 `ExamService`（创建考试、查询列表、时间校验） — A
- [x] 编写 `ExamController`（GET/POST/DELETE `/api/exams`） — A
- [x] 用 Postman 测试所有接口 — A + B

---

### 第 4 天 — 6/24（三）：组卷 + 判卷 + 统计（核心）

> 进度：**6/6** ✅

- [x] 组卷接口 `GET /api/exams/{id}/paper`（抽题 + `Collections.shuffle` 随机打乱，持久化到 questions 表） — A
- [x] 提交接口 `POST /api/exams/{id}/submit`（比对答案、判对错、算分） — A
- [x] 成绩查询接口 `GET /api/scores?examId=` — A
- [x] 统计接口 `GET /api/stats/{examId}`（平均分、最高分、各题正确率） — A
- [x] 字符云数据接口 `GET /api/wordcloud?userId=`（返回错词及频次） — A
- [x] 管理员成绩统计页所需的数据接口全部就绪 — A

---

### 第 5 天 — 6/25（四）：前端基础 + 管理端页面

> 进度：**4/4** ✅

- [x] 搭建前端框架：导航栏 HTML 模板 + `api.js` 封装（fetch 统一处理） — B
- [x] 登录页 `login.html` — B
- [x] 单词管理页 `admin/words.html`（表格 + 搜索 + 新增/编辑弹窗） — B
- [x] 考试管理页 `admin/exams.html`（创建表单 + 列表 + 删除） — B

---

### 第 6 天 — 6/26（五）：学生端页面 + 统计页

> 进度：**4/4** ✅

- [x] 学生首页 `student/dashboard.html`（考试列表 + 历史成绩） — B
- [x] 答题页 `student/exam.html`（题目展示 + 倒计时 + 提交） — A
- [x] 交卷结果页 `student/result.html`（分数 + 对错详情） — A
- [x] 成绩统计页 `admin/stats.html`（成绩表 + 统计数字） — B

---

### 第 7 天 — 6/27（六）：字符云 + 选择题 + 联调修 bug

> 进度：**3/4**

- [x] 字符云页 `admin/wordcloud.html`（调字符云数据接口，渲染词云） — B
- [x] 选择题题型完善（后端随机生成 3 个干扰选项） — A
- [x] 前后端联调，修 bug（状态字段补充、字段名对齐） — A + B
- [ ] 阿里巴巴规约扫描，清除 warning — A + B

---

### 第 8 天 — 6/28（日）：报告 + PPT + 模拟答辩

> 进度：**1/4**

- [ ] 编写个人课程设计报告 — A + B
- [ ] 制作团队演示 PPT（约 10 页） — A + B
- [x] 编写 README.md — A
- [ ] 模拟答辩（互相讲解自己模块的代码） — A + B

---

### 6/29（一）提交

- [ ] `mvn package` 打 jar，压缩项目文件夹，学委汇总上交

---
